# 👃 Code Smell: Message Chains

**อ้างอิงข้อมูลและตัวอย่างจาก:** [Refactoring.guru](https://refactoring.guru/smells/message-chains)

---

## a) ความหมายของ Message Chains

**Message Chains** (ห่วงโซ่ของข้อความ) เกิดขึ้นเมื่อ Client (ผู้เรียกใช้งาน) เริ่มต้นนำทางผ่านโครงสร้างของ Object หลายๆ ตัวต่อกันเพื่อเข้าถึงข้อมูลที่ต้องการ

**ลักษณะอาการ:**
> โค้ดมีการเรียก Method ต่อกันเป็นทอดๆ ยาวเหยียด เช่น `obj.getA().getB().getC()`

**ปัญหาที่เกิดขึ้น:**
การทำแบบนี้จะทำให้ Client ผูกติด (**Coupled**) กับโครงสร้างความสัมพันธ์ของ Class เหล่านั้นมากเกินไป หากมีการเปลี่ยนความสัมพันธ์ (เช่น `A` ไม่ได้ชี้ไปที่ `B` แล้ว) ฝั่ง Client จะต้องตามแก้โค้ดทั้งหมด

---

## b) ตัวอย่างโค้ดที่เกิด Code Smell

ในทางปฏิบัติ ตัวอย่างที่ชัดเจนที่สุดคือการเรียกใช้ getter ต่อกันเป็นสายโซ่ยาวๆ

```php
// ตัวอย่างรูปแบบ Code Smell (Message Chains)
// Client ต้องรู้โครงสร้างภายในทั้งหมดว่า A มี B, B มี C, และ C มี D

$a->b()->c()->d();

```

## c) ตัวอย่างโค้ดที่ Refactoring แล้ว
**วิธีแก้ไขหลัก** ใช้เทคนิค Hide Delegate (ซ่อนผู้ถูกมอบหมายงาน) โดยการย้ายตรรกะการเรียกนั้นเข้าไปไว้ใน Object ต้นทาง
หลักการ: แทนที่จะให้ Client วิ่งไปหาเอง เราสร้าง Method ใหม่ที่ "ห่อหุ้ม" การทำงานนั้นไว้

```php
// ตัวอย่างโค้ดหลัง Refactoring (Clean Code)
// เราเรียกใช้ method ที่เตรียมไว้ให้โดยตรง ตัดตอนการเรียกที่ซับซ้อนออกไป

$a->d();
```
ในการทำจริง ภายใน Class ของ $a จะมี Method d() ที่เขียนว่า return this.b.c.d(); เพื่อซ่อนความซับซ้อนนี้จาก Client

## d) กรณีที่ Code Smell นี้ไม่ได้บ่งบอกถึงปัญหา
(When to Ignore)

การลบ Message Chains บางครั้งอาจก่อให้เกิด Code Smell อีกชนิดหนึ่งแทน นั่นคือ **Middle Man**

**Middle Man Smell:** เกิดขึ้นเมื่อเราพยายามซ่อน Delegate มากเกินไป จน Class ตัวกลางกลายเป็นเพียง "นายหน้า" ที่ไม่ได้ทำหน้าที่อะไรเลยนอกจากส่งต่อคำสั่งไปยัง Object อื่น

**ข้อพิจารณา:** หากการ Refactor ทำให้ Class กลายเป็น Middle Man มากเกินไป เราอาจจำเป็นต้องยอมปล่อยให้มี Message Chains บ้างในบางจุด หรือพิจารณาใช้เทคนิคอื่นแทน เช่น Extract Method หรือ Move Method เพื่อย้ายโค้ดไปอยู่ในที่ที่เหมาะสมแทนการซ่อน Delegate

